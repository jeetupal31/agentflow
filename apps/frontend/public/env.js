// Default (local dev). In Docker/production the container regenerates this file
// at startup from runtime env vars. Empty values ⇒ the app falls back to localhost.
window.__ENV__ = window.__ENV__ || {};
